interface CompareToEncryptedProps {
    receivedString: string;
    encryptedString: string;
}
export declare const compareToEncrypted: ({ receivedString, encryptedString, }: CompareToEncryptedProps) => boolean;
export {};
